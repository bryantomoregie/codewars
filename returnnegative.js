function makeNegative(num) {
//if number is negative, return number.
//if number is positive, negatify it and return it
  }

  function makeNegative(num) {
    if(num < 0 ) {
        return num 
    } else {
        return -Math.abs(num) 
    }
  }

  // what did you learn?
  // continued practice with if else
  //-Math.abs(num) returns only a negative number 
  //the ternary expression is better ? :
