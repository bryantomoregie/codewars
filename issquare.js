var isSquare = function(n){
    //take in a number 
    // determine if square 
   //if number is square, return true 
   // if number is not square returne false
  }

  var isSquare = function(n){
    if(Math.sqrt(n) % 1 === 0){
        return positve
    } else {
        return negative 
    }
  }

  //what I learned 
  //Javascript math object allows us to mathematical tasks on numbers
  //in this case I used Math.sqrt() to fnid the square root of n
  //also, this returns a value. So I couldve just said return, and had code in one line